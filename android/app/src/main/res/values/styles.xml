<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- windowLayoutInDisplayCutoutMode="shortEdges" below (both styles) lets
         the Activity's window extend into a display cutout/punch-hole in
         portrait, matching what App.tsx's StatusBar.setOverlaysWebView({
         overlay: true }) + Navbar.tsx's `padding-top: env(safe-area-inset-
         top)` already assume. Without it, Android's default cutout mode
         reserves the cutout area as a separate letterboxed strip on some
         devices/OEM skins instead of reporting it through the normal
         safe-area inset, so the WebView's actual top edge ends up higher
         than what env(safe-area-inset-top) told it to pad for — on a tall,
         high-density display in particular (e.g. a 20:9 1260x2800 panel)
         that gap shows up as the status bar clock/icons overlapping the
         app's own header instead of sitting above it. Attribute needs API
         27+ (Android 8.1); silently ignored on the minSdkVersion=22 floor
         below that, where there's no display cutout to worry about anyway. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="27">shortEdges</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="27">shortEdges</item>
    </style>
</resources>